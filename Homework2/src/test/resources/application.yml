spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: data_test.sql

  shell:
    interactive:
      enabled: false
  main:
    allow-bean-definition-overriding: true